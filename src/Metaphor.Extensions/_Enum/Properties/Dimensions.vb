Friend NotInheritable Class Dimensions
    Private Sub New() : End Sub
    Friend Const HEADING As String = NameOf(HEADING)
    Friend Const SPEED As String = NameOf(SPEED)
    Friend Const X As String = NameOf(X)
    Friend Const Y As String = NameOf(Y)
End Class
